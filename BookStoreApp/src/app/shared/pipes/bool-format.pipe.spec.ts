import { BoolFormatPipe } from './bool-format.pipe';

describe('BoolFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
